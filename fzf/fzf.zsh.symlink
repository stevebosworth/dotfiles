# Setup fzf
# ---------
if [[ ! "$PATH" == */Users/steveb/.fzf/bin* ]]; then
  PATH="${PATH:+${PATH}:}/Users/steveb/.fzf/bin"
fi

# Auto-completion
# ---------------
[[ $- == *i* ]] && source "/Users/steveb/.fzf/shell/completion.zsh" 2> /dev/null

# Key bindings
# ------------
source "/Users/steveb/.fzf/shell/key-bindings.zsh"
